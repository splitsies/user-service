name: Deploy Dev

on:
  pull_request:
    branches:
      - main
      - releases/**

jobs:
  deploy:
    name: deploy
    runs-on: ubuntu-latest
    strategy:
      matrix:
        node-version: [18.x]
    
    steps:
    - uses: actions/checkout@v3
    - name: Use Node.js ${{ matrix.node-version }}
      uses: actions/setup-node@v3
      with:
        node-version: ${{ matrix.node-version }}

    - name: Create Common Parameter Overrides
      run: | 
        params=""
        params="Stage=dev ${params}"
        params="QueueStageName=dev ${params}"
        params="messageQueueResourceName=MessageQueue ${params}"
        params="dbTableName=User ${params}"
        params="dbRegion=us-east-1 ${params}"
        params="dbEndpoint=https://dynamodb.us-east-1.amazonaws.com/ ${params}"
        params="ResourceAccountId=${{ secrets.DEV_ACCOUNT_ID }} ${params}"
        params="MessageQueueStreamArn=${{ secrets.MESSAGE_QUEUE_ARN_DEV_PR }} ${params}"
        params="FirebaseApiKey=${{ secrets.FIREBASE_API_KEY_DEV_PR }} ${params}"
        params="FirebaseAuthDomain=${{ secrets.FIREBASE_AUTH_DOMAIN_DEV_PR }} ${params}"
        params="FirebaseProjectId=${{ secrets.FIREBASE_PROJECT_ID_DEV_PR }} ${params}"
        params="FirebaseStorageBucket=${{ secrets.FIREBASE_STORAGE_BUCKET_DEV_PR }} ${params}"
        params="FirebaseMessagingSenderId=${{ secrets.FIREBASE_MESSAGING_SENDER_ID_DEV_PR }} ${params}"
        params="FirebaseAppId=${{ secrets.FIREBASE_APP_ID_DEV_PR }} ${params}"
        params="FirebaseMeasurementId=${{ secrets.FIREBASE_MEASUREMENT_ID_DEV_PR }} ${params}"
        params="FirebaseAdminCreds=${{ secrets.FIREBASE_ADMIN_CREDS_JSON_DEV_PR }} ${params}"
        params="ResourceAccountId=${{ secrets.DEV_ACCOUNT_ID }} ${params}"
        echo $params > parameters.cfg

    - uses: actions/setup-python@v3
    - uses: aws-actions/setup-sam@v2
    - uses: aws-actions/configure-aws-credentials@v1
      with:
        aws-access-key-id: ${{ secrets.SPLITSIES_DEPLOY_ACCESS_KEY_ID_DEV }}
        aws-secret-access-key: ${{ secrets.SPLITSIES_DEPLOY_SECRET_KEY_DEV }}
        aws-region: us-east-1

    - run: npm ci

    - run: npm run build

    - name: Deploy to us-east-1 (SAM Deploy)
      run: |
        PARAMETERS=$(cat parameters.cfg)
        sam deploy --stack-name user-service \
        --region us-east-1 \
        --no-confirm-changeset \
        --no-fail-on-empty-changeset \
        --parameter-overrides $PARAMETERS

    - run: npm ci
      working-directory: ./database
      
    - run: sam build
      working-directory: ./database

    - run: |
        sam deploy --stack-name user-service-database \
        --no-confirm-changeset \
        --no-fail-on-empty-changeset \
        --parameter-overrides "dbRegion=us-east-1" "StagingLambdaExecutionRoleArn=${{ secrets.STAGING_USER_SERVICE_LAMBDA_EXECUTION_ROLE_ARN }}"
      working-directory: ./database
